
---
import { db } from '../lib/firebase.js';
import { doc, getDoc } from 'firebase/firestore';

// Define image paths
const dianaImageUrl = '/images/diana.png';

// Define a complete, default structure for the content
const defaultContent = {
  site: { title: "5091" },
  header: { title: "5091" },
  hero: { title: "Bienvenido", subtitle: "Un subtítulo increíble" },
  about: { title: "Sobre Nosotros", text: "Nacimos de la locura y las ganas de pasarlo bien. Somos más que un bar: somos tu punto de encuentro, tu segunda casa, el lugar donde las anécdotas nacen y las noches se vuelven legendarias. Con la mejor música, un ambiente que te atrapa y una familia que no para de crecer, te invitamos a ser parte de nuestra historia. ¿Te apuntas a la próxima aventura?"},
  gallery: { title: "Galería", images: [] },
  events: { title: "Eventos", list: [] },
  footer: { title: "Contacto", address: "", schedule: "", social: [] }
};

let content = defaultContent;

try {
  const docRef = doc(db, 'content', 'pageData');
  const docSnap = await getDoc(docRef);

  if (docSnap.exists()) {
    const remoteData = docSnap.data() || {};
    content = {
      site: { ...defaultContent.site, ...remoteData.site },
      header: { ...defaultContent.header, ...remoteData.header },
      hero: { ...defaultContent.hero, ...remoteData.hero },
      about: { ...defaultContent.about, ...remoteData.about },
      gallery: { ...defaultContent.gallery, ...remoteData.gallery },
      events: { ...defaultContent.events, ...remoteData.events },
      footer: { ...defaultContent.footer, ...remoteData.footer },
    }
    if (!Array.isArray(content.gallery.images)) content.gallery.images = [];
    if (!Array.isArray(content.events.list)) content.events.list = [];
    if (!Array.isArray(content.footer.social)) content.footer.social = [];
  } else {
    console.log("No such document! Using default content.");
  }
} catch (error) {
    console.error("Failed to fetch data from Firestore. Using default content.", error);
    content = defaultContent;
}

// Statically define navigation items to ensure they match page structure
const navItems = [
  { name: "Galería", href: "#gallery" },
  { name: "Eventos", href: "#events" },
  { name: "Contacto", href: "#contact" }
];

--- 
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{content.site.title}</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Permanent+Marker&display=swap" rel="stylesheet">
    <style define:vars={{ bgImage: `url(${dianaImageUrl})` }}>
      body {
        background-image: var(--bgImage);
        background-size: cover;
        background-attachment: fixed;
        background-position: center;
      }
    </style>
    <style>
      html {
        scroll-behavior: smooth;
      }
      .font-logo {
        font-family: 'Lobster', cursive;
      }
      .font-gamberro {
        font-family: 'Permanent Marker', cursive;
      }
      .gallery-item-container {
        overflow: hidden;
        border-radius: 0.5rem;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        background-color: rgba(0, 0, 0, 0.5);
      }
      .gallery-image {
        width: 100%;
        height: 16rem;
        object-fit: cover;
        transition: transform 0.35s ease-in-out;
      }
      .gallery-item-container:hover .gallery-image {
        transform: scale(1.1);
      }
    </style>
</head>
<body class="bg-black text-white font-sans">
    <div class="fixed inset-0 bg-black bg-opacity-30 z-[-1]"></div>

    <div class="relative z-1">
        <header class="p-4 flex justify-between items-center">
            <a href="/" class="text-5xl font-logo text-yellow-400 hover:text-yellow-300 transition-colors">{content.header.title}</a>
            <nav>
                {navItems.map(item => (
                    <a 
                        href={item.href} 
                        class="mx-2 text-lg md:text-xl hover:text-yellow-400 transition-colors md:mx-4 font-gamberro"
                    >
                        {item.name}
                    </a>
                ))}
            </nav>
        </header>

        <main>
            <section id="hero" class="py-12 md:py-20 flex items-center justify-center text-center">
                <div>
                    <h2 class="text-6xl font-extrabold text-white transition-colors duration-300 hover:text-yellow-400 font-gamberro" style="text-shadow: 2px 2px 4px rgba(0,0,0,0.7);">{content.hero.title}</h2>
                    <img 
                        src="/images/logo.png" 
                        alt="Logo de 5091" 
                        class="h-72 md:h-96 mx-auto mt-6"
                        style="background-color: transparent !important;"
                    />
                    <div class="mt-8">
                        <a href="#events" class="bg-yellow-400 text-gray-900 font-gamberro text-2xl px-8 py-3 rounded-md transition-transform duration-300 hover:scale-105 hover:bg-yellow-300 shadow-lg">
                            Ver Eventos
                        </a>
                    </div>
                </div>
            </section>

            <section id="about-us" class="py-10 px-4 bg-black bg-opacity-40">
                <h3 class="text-4xl font-bold text-center mb-12 transition-colors duration-300 hover:text-yellow-400 font-gamberro">{content.about.title}</h3>
                <div class="max-w-5xl mx-auto flex flex-col md:flex-row items-center gap-8">
                    <div class="md:w-1/2">
                        <img src="/images/about.jpg" alt="Sobre Nosotros" class="rounded-lg shadow-lg w-full">
                    </div>
                    <div class="md:w-1/2">
                        <p class="text-lg text-left transition-colors duration-300 hover:text-yellow-400">
                            {content.about.text}
                        </p>
                    </div>
                </div>
            </section>

            <section id="gallery" class="py-10 px-4 bg-black bg-opacity-30">
                <h3 class="text-4xl font-bold text-center mb-12 transition-colors duration-300 hover:text-yellow-400 font-gamberro">{content.gallery.title}</h3>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                    {content.gallery.images.map(image => (
                        <div class="gallery-item-container">
                            <img 
                                src={image} 
                                alt={`Galería de ${content.site.title}`} 
                                class="gallery-image"
                            />
                        </div>
                    ))}
                </div>
            </section>

            <section id="events" class="py-10 px-4 bg-gray-900 bg-opacity-50">
                <h3 class="text-4xl font-bold text-center mb-8 transition-colors duration-300 hover:text-yellow-400 font-gamberro">{content.events.title}</h3>
                <div class="max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8">
                    {content.events.list.map(event => (
                        <div class="bg-gray-800 bg-opacity-90 p-6 rounded-lg">
                            <h4 class="text-2xl font-bold mb-2 transition-colors duration-300 hover:text-yellow-400">{event.title}</h4>
                            <p class="text-yellow-400 mb-4">{event.schedule}</p>
                            <p class="transition-colors duration-300 hover:text-yellow-400">{event.description}</p>
                        </div>
                    ))}
                </div>
            </section>
        </main>

        <footer id="contact" class="bg-gray-900 bg-opacity-50 py-8 px-4 text-center">
            <h3 class="text-3xl font-bold mb-4 transition-colors duration-300 hover:text-yellow-400 font-gamberro">{content.footer.title}</h3>
            <p class="mb-2 transition-colors duration-300 hover:text-yellow-400">{content.footer.address}</p>
            <p class="mb-2 transition-colors duration-300 hover:text-yellow-400">{content.footer.schedule}</p>
            <p class="mb-4">
                Síguenos en 
                {content.footer.social.map((link, index) => (
                    <>
                        <a href={link.url} target="_blank" rel="noopener noreferrer" class="text-yellow-400 hover:underline">{link.name}</a>
                        {index < content.footer.social.length - 1 && ' y '}
                    </>
                ))}
            </p>
        </footer>
    </div>
</body>
</html>
